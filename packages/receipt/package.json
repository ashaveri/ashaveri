{
  "name": "@ashaveri/receipt",
  "version": "0.1.0",
  "description": "Deterministic CBOR + COSE_Sign1 receipt codec for verifiable AI inference",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "receipt.cddl",
    "schemas"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^2.0.0",
    "cbor2": "^2.3.0"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
