{
  "name": "@ashaveri/fixtures",
  "version": "0.1.0",
  "description": "Golden conformance vectors for Ashaveri receipt verification (shared across TS, Python, and CLI implementations)",
  "license": "CC0-1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "data"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json && tsc -p tsconfig.scripts.json",
    "generate": "node --experimental-strip-types scripts/generate.ts",
    "generate:pop": "node --experimental-strip-types scripts/pop-vectors.ts",
    "generate:req": "node --experimental-strip-types scripts/req-vectors.ts",
    "generate:res": "node --experimental-strip-types scripts/res-vectors.ts",
    "generate:marking": "node --experimental-strip-types scripts/marking-vectors.ts",
    "generate:chain": "node --experimental-strip-types scripts/chain-vectors.ts",
    "generate:export": "node --experimental-strip-types scripts/export-vectors.ts",
    "generate:manifest": "node --experimental-strip-types scripts/manifest-vectors.ts",
    "generate:pack": "node --experimental-strip-types scripts/pack-vectors.ts",
    "generate:redaction": "node --experimental-strip-types scripts/redaction-vectors.ts"
  },
  "dependencies": {
    "@ashaveri/receipt": "workspace:*",
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^2.4.0"
  },
  "devDependencies": {
    "@ashaveri/signerd": "workspace:*",
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
