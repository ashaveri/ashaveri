# Contracts

No current plan for contracts as of this commit.

The directory is empty. ashaveri issues signed receipts for AI inference, and the schema that describes them is the receipt format at `../../receipt/receipt.cddl`.

This says what is true today and takes no position on tomorrow. On-chain work would land here if it is ever taken on, and a directory that stays empty is one to delete.
